spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crud_asilo
    username: postgres
    password: ${Senha_Secreta}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true



